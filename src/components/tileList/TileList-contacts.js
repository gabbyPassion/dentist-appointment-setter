/*import React from "react";
import { Tile } from "../tile/Tile";

export const TileList = ({ tiles }) => {
  if (!tiles) {
    // Handle the case where tiles is undefined or null
    return <p>No changes yet.</p>;
  }

  return (
    <div>
      {tiles.map((tile, index) => {
        const { title, ...appointment } = tile;
        return <Tile key={index} title={title} appointment={appointment}
        />;
      })}
    </div>
  );
}; */

